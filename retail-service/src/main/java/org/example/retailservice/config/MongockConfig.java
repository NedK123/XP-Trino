package org.example.retailservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MongockConfig {
  //    @Bean
  //    public SpringBootMongock mongock(MongoTemplate mongoTemplate, ApplicationContext
  // springContext) {
  //        return new SpringBootMongockBuilder(mongoTemplate, "changeLogScanPackage")
  //                .setApplicationContext(springContext)
  //                .setLockQuickConfig() OR .setLockConfig(3, 4, 3)
  //                .build();
  //    }
}
